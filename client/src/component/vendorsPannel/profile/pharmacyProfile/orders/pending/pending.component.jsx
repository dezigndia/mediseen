import React from 'react';
import './pending.styles.scss';

const Pending = () => { 
    return (
        <div className="pendingOrders">
            pending
        </div>
    );
}

export default Pending;